<% if current_user %>
<h2>Your courses</h2>
  <% if current_user.streams.count > 0 %>
    <%= render current_user.streams %>
  <% else %>
    Nothing yet!
  <% end %>

  <%= render 'form' %>
  <hr>
<% end %>

<h2>Courses on Learnstream</h2>
<%= render @streams %>
